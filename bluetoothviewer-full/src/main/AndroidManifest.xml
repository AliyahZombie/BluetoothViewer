<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name="net.bluetoothviewer.application.BluetoothViewerFullApplication"
        android:allowBackup="true"
        android:icon="@drawable/launcher_main"
        android:label="@string/app_name"
        android:theme="@style/Theme.BluetoothViewer">
        <activity
            android:name="net.bluetoothviewer.BluetoothViewer"
            android:exported="true"
            android:windowSoftInputMode="stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="net.bluetoothviewer.DeviceListActivity"
            android:configChanges="orientation|keyboardHidden"
            android:label="@string/title_select_device"
            android:exported="false"
            android:theme="@style/Theme.BluetoothViewer.Dialog" />
        <activity
            android:name="net.bluetoothviewer.SettingsActivity"
            android:configChanges="orientation|keyboardHidden"
            android:label="@string/title_settings_activity"
            android:exported="false"
            android:theme="@style/Theme.BluetoothViewer.Dialog" />
    </application>

</manifest>
